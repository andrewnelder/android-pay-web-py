{
  "name": "Apple Pay for the Web Example",
  "description": "Play with Apple Pay for the Web",
  "repository": "https://github.com/andrewnelder/apple-pay-web-py",
  "keywords": ["flask", "python", "stripe"],
  "env": {
    "STRIPE_PUBLISHABLE_KEY": {
      "description": "Find this at https://dashboard.stripe.com/account/apikeys (it'll look like pk_test_****)",
      "required": true
    },
    "STRIPE_SECRET_KEY": {
      "description": "Find this at https://dashboard.stripe.com/account/apikeys (it'll look like sk_test_****)",
      "required": true
    }
  }
}
